{
  "id": "PI-JSON-EXIT-STATUS-001",
  "title": "JSON 模式将 provider 错误报告为成功退出",
  "predicate": {
    "id": "nonzero_exit_after_provider_error",
    "type": "event_count",
    "eventType": "successful_exit_after_provider_error",
    "expected": 0
  },
  "expected": "provider 已报告错误时，非交互 Pi 必须以非零退出码结束",
  "runner": "run.mjs",
  "runtime": {
    "kind": "pi",
    "candidate": { "command": "pi" },
    "baseline": { "command": "pi" }
  },
  "candidate": {
    "mode": "json"
  },
  "baseline": {
    "mode": "text"
  }
}