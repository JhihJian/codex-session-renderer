# 项目质量演进记录

## 2026-07-20 第三十六轮：受限诊断分页快照失效收口

- 发现：分页续页的结构化 `409/session_snapshot_changed` 仍落入通用失败路径，只清空摘要页；当前事件选择和已缓存的完整事件保留，和单事件入口的快照失效语义不一致，可能让用户在失效快照的选择或完整内容上继续操作。
- 交付：分页续页现在与单事件入口共用同一个快照变化清理与回执。只有当前诊断代际、来源/会话和快照均匹配的结构化 `409` 才清空页、选择和完整事件缓存；普通失败仍保留原有通用失败行为，取消、重启后的迟到响应及来源/会话/视图切换继续被当前请求校验隔离。
- 验证：新增 Chromium 回归先缓存完整事件，再让分页续页返回 `409`，断言统一回执、零摘要行和受限会话复核上下文；重新开始后用相同快照再次展开同一事件，确认完整事件请求重新发出而非复用旧缓存。`npm run verify` 通过（256 项 Node、50 项 Chromium），`git diff --check` 通过。

## 2026-07-20 第三十五轮：受限诊断重启代际隔离

- 发现：用户在受限诊断中点击“重新开始”后，旧单事件读取没有被取消；它若随后以旧 `snapshot` 返回 `409/session_snapshot_changed`，前端只按来源和会话判断清理资格，能错误清空已成功读取的新快照摘要、选择和完整事件缓存。
- 交付：显式重新开始现在同时中止在途诊断分页和单事件读取，并替换诊断状态实例。单事件响应写缓存、以及结构化 `409` 触发的清理，均额外校验请求未中止、同一诊断状态实例和同一快照；旧代际即使在取消竞争下迟到也只能被忽略。当前代际、当前快照的真实 `409` 仍按原语义清空过期状态；普通失败、来源/会话/视图切换和既有资源边界不变。
- 验证：新增独立 Chromium 回归覆盖“重新开始”实际中止在途单事件请求，以及受控取消竞争下旧快照 `409` 在新快照摘要成功显示后抵达仍不清空页面，并在旧响应完成后的两个渲染帧后复核新摘要和选择列表仍在。`npm run verify` 通过（256 项 Node、49 项 Chromium），`git diff --check` 通过。

## 2026-07-20 第三十四轮：受限诊断单事件快照一致性

- 发现：受限会话的原始事件分页已经在快照变化时清空旧页，但复核台按需展开单事件收到同一结构化 `409/session_snapshot_changed` 时只显示普通失败，仍保留旧摘要、选择和完整事件缓存；用户可继续在已过期内容之间切换，违反快照变化不混合版本的约束。
- 交付：分页与单事件展开共用快照变化恢复语义。当前来源和会话的单事件 `409/session_snapshot_changed` 会清空诊断页、当前选择和完整事件缓存，并给出“重新开始读取”的可恢复回执；普通单事件失败、取消及来源/会话切换不触发该清理。网络取得的新页也与缓存前进/后退一致地清空上一页事件选择。
- 验证：新增独立 Chromium 回归依次覆盖普通单事件 `500` 保留当前诊断页、正常展开写入缓存、结构化 `409` 清空页/选择/缓存、无快照令牌重启和相同令牌下重新请求完整来源，证明缓存未复用。服务端 HTTP 回归继续断言分页与单事件接口均返回 `409/session_snapshot_changed`；`npm run verify` 通过（256 项 Node、47 项 Chromium），`git diff --check` 通过。

## 2026-07-20 第三十三轮：受限诊断分页缓存复用

- 发现：受限会话的有界原始事件诊断在第 2 页返回第 1 页后，仍会以第 1 页的 `nextCursor` 再次请求第 2 页，并把响应追加成伪第 3 页。这会破坏页号、重复缓存同一摘要页，并无谓消耗 JSONL 读取预算。
- 交付：前端前进优先复用同一会话、来源与快照诊断状态中的相邻缓存页，只有缓存未命中时才请求服务端游标；缓存仍最多保留 6 页。响应写入同时校验请求 cursor、快照和既有 cursor，不一致时清空诊断并要求重新开始，防止异常或重复响应混入。缓存页保留稳定逻辑页号；普通列表刷新也与远端快照刷新一致，会中止并清空在途诊断和单事件读取；显式重启、会话/来源/视图切换、取消、失败重试和快照变化继续使用既有清空与取消路径。
- 验证：新增独立 Chromium 回归，覆盖第 1 页 -> 第 2 页 -> 第 1 页 -> 第 2 页；网络仅出现 `cursor=0` 与 `cursor=100`，回到第 2 页不产生请求也不伪造第 3 页；另覆盖列表刷新中止在途第 2 页请求并清空旧缓存。`npm run verify` 通过：256 项 Node、46 项 Chromium；`git diff --check` 通过。

## 2026-07-18 第二十七轮：移动详情导航与远端历史可达性

- 发现：普通会话详情的迟到响应会无条件切回移动内容面板，覆盖用户请求期间主动切换到复核面板的导航；远端历史的仅索引元数据行没有正文却伪装为禁用按钮，101 条结果会占满键盘 Tab 路径，Enter/空格还会给出激活反馈。
- 交付：会话选择和迟到响应分别处理。用户选择可打开会话时立即进入内容面板，详情请求记录移动面板的用户导航版本；请求期间用户切换面板后，成功响应只写入详情，不再改写面板。远端仅索引行改为带“正文未同步，无法直接打开”读屏名称的静态列表项，保留元数据、鼠标说明和历史分页，不再带按钮、禁用按钮或键盘激活语义。
- 验证：定向 Chromium 覆盖普通会话迟到详情保留复核、未导航时进入内容，以及 office 101 条仅索引行的静态语义、Tab 路径与分页。`npm run verify` 通过：254 项 Node、33 项 Chromium；`git diff --check` 通过。

## 2026-07-18 第二十六轮：来源与导航上下文隔离

- 发现：桌面任务归档仅用 CSS 收起复核台，内部 tab 仍可聚焦；内容类型筛选没有稳定可访问名称；前端归档缓存和远端刷新完成缺少完整的来源/导航上下文围栏。
- 交付：桌面归档将复核台设为 `inert` 与 `aria-hidden`，顶部开关在不可用时禁用且离开后恢复原开闭状态，移动端归档复核路径保持可用；内容类型筛选固定命名为“内容类型筛选”；归档缓存键包含来源，列表、详情和远端索引拒绝来源不匹配响应，远端刷新只在原来源及导航上下文仍有效时回写。
- 验证：新增 Chromium 回归覆盖桌面焦点/恢复、移动保护、四视图筛选名称和来源切换后的迟到刷新；完整 `npm run verify` 通过（254 项 Node、24 项 Chromium）。

## 2026-07-17 第一轮：局域网访问边界

- 发现：非 loopback 工作台曾无访问控制，同网段访问者可读取会话和远端来源信息或触发管理操作。
- 交付：提交 `6be4d5f`。非 loopback 监听要求 `CODEX_SESSION_RENDERER_TOKEN`；静态资源和 API 统一认证，配置私有原子写入，README 与 systemd 示例同步收紧。
- 验证：`npm run lint`、`npm test`（187 项）通过。

## 2026-07-17 第二轮：任务归档资源边界

- 发现：归档会对最多 800 个 JSONL 无界并发并完整读取，120 个 512 KiB 首提示可产生约 60 MiB 响应和约 570 MiB RSS。
- 交付：提交 `10655f5`。候选/返回上限 200、读取并发 4、缓存 400；限制单文件、记录数和提示词，文件版本校验与订阅级取消。
- 验证：`npm run lint`、`npm test`（195 项）通过。

## 2026-07-17 第三轮：Chromium 工作台契约

- 发现：README 承诺响应式面板、键盘 tablist、焦点恢复、复核台调宽和归档取消，但没有浏览器回归。
- 交付：提交 `4928193`。新增隔离 Playwright Chromium、固定 JSONL 夹具和临时服务；覆盖桌面/移动布局、ARIA、焦点恢复、归档取消和陈旧响应。
- 验证：`npm run lint`、`npm test`（194 项）、`npm run test:e2e`（4 项）通过。

## 2026-07-17 第四轮：会话详情资源边界

- 发现：详情、外部 view、Markdown 和原始事件会无界读取 JSONL，详情缓存无限期；3 MiB 用户消息可返回约 6.3 MiB。
- 交付：提交 `9078f46`。统一限制 8 MiB、10,000 事件、并发 4、LRU 24 条/48 MiB；版本变化、超限、失败或中止不返回局部详情或写入缓存。
- 验证：`npm run lint`、`npm test`（199 项）、`npm run test:e2e`（4 项）通过。

## 2026-07-17 第五轮：远端快照传输与共享建包

- 发现：等价快照请求重复复制和打包；下载、索引、健康检查缺少 deadline、字节预算和端到端取消；旧 `current` 切换有不可见窗口。
- 交付：提交 `5616d23`。远端请求采用 deadline、长度预检、流式字节限制和取消；共享请求合并并限队列，发布改为不可变目录与原子 `current` 指针。
- 验证：`npm run lint`、`npm test`（206 项）、`npm run test:e2e`（4 项）通过。

## 2026-07-17 第六轮：快照内容展开预算

- 发现：建包、解包缺少展开字节、文件数和 deadline 预算，异常压缩比归档或 `scope=all` 可耗尽临时磁盘。
- 交付：提交 `1622fd8`。限制总字节、文件数、产物和时间；拒绝压缩炸弹、链接、路径逃逸、重复及非白名单条目，异常时清理临时目录并保留旧快照。
- 验证：`npm run lint`、`npm test`（212 项）、`npm run test:e2e`（4 项）、`npm run verify` 通过。

## 2026-07-17 第七至九轮：远端来源版本与最终发布

- 发现：远端地址或快照路径变更后可继续展示旧 `current`；配置原子替换前失效围栏会隔离旧快照；最终来源校验与异步切换间可能被旧任务覆盖。
- 交付：来源版本不含 token，旧版本的列表、详情、归档、复核、查询和 Markdown 统一 `409`；配置成功后更新围栏，状态私有原子替换；按快照根的最终提交槽使来源判定与 `current` 切换线性化。
- 验证：覆盖改址、删除、配置替换失败、损坏状态恢复及不同根并行发布。

## 2026-07-18 第十轮：受限会话的工作台内有界诊断

- 发现：详情受 8 MiB/10,000 事件限制后会跳转裸 JSON；分页在字节边界可能不推进，且无令牌会混合快照变化前后的页面。
- 交付：工作台内用服务端游标分页诊断，最多保留 6 页，opaque `snapshot` 不匹配返回 `409`；独立诊断扫描上限默认 100,000，切换与重试取消旧请求并隔离陈旧响应。
- 验证：Node 覆盖 snapshot、游标、摘要脱敏、`409`、字节边界和扫描上限；Chromium 覆盖诊断、翻页、按需读取、取消和陈旧响应。

## 2026-07-18 第十一轮：远端历史索引完整性

- 发现：SQLite 查询静默截断 1000 条，数值 cursor 会在索引变化后混合页面，查询与传输缺少完整有界和取消约束。
- 交付：count、筛选和稳定分页下推 DB；opaque snapshot 续页不匹配返回 `409`；查询和传输补齐 deadline、取消与回退预算。
- 验证：`npm run verify` 通过：230 项 Node、9 项 Chromium。

## 2026-07-18 第十二轮：任务归档范围透明与继续定位

- 发现：上游在 200 条候选处截断，当前批可能误报完整或无匹配，实际命中可在更早候选。
- 交付：归档使用有界候选分页与 opaque 令牌；页面显示扫描范围和完成态，范围变化返回 `409` 后从首批重启，并修复前端取消和陈旧响应隔离；README 同步。
- 验证：`npm run verify` 通过：231 项 Node、11 项 Chromium；服务重启后健康。

## 2026-07-18 第十三轮：远端快照会话路径边界

- 发现：远端 SQLite 的 `rollout_path` 是不可信定位线索；若映射可越出已发布快照会话根，恶意或错误元数据可诱导服务读取本机任意 JSONL。
- 交付：远端映射失败关闭，只接受严格映射至 `current/sessions/**/*.jsonl` 的路径；在 SQLite 查询、文件回退、详情、Markdown、原始事件、归档和子代理展开前统一复核 `realpath` 仍在会话根、所有路径分量无链接、目标为普通 JSONL，且文件名会话 ID 与 SQLite/文件记录一致。拒绝绝对路径、`..`、`archived_sessions`、非 JSONL、符号链接、快照外路径及 ID 不一致记录，不回显原始路径、预览或内容；README 同步该约束。
- 验证：`npm run verify` 通过：232 项 Node、11 项 Chromium；`GET http://127.0.0.1:4789/api/health` 返回 `ok:true`，`codex-session-renderer.service` 为 active。
- 下一轮建议：优先修复已复现的 71 `--limit` 在正式目标删除未枚举会话问题，再评估无障碍体验。

## 2026-07-18 第十四轮：71 限量同步发布安全

- 发现：完整目标使用 `--limit` 曾删除未枚举会话，并可能发布不完整快照。
- 交付：限量同步仅允许空或部分测试目标；拒绝既有完整或状态未知目标及 `--refresh`。限量结果明确标记为 `partial`，数据源拒绝其发布；全量同步才恢复为 `complete`。
- 验证：`npm run verify` 通过：234 项 Node、11 项 Chromium；服务健康。
- 下一轮建议：推进无障碍异步状态、浅色对比度和移动三面板语义；其他尚未实证的 71 硬链接结论暂缓。

## 2026-07-18 第十五轮：远端同步与归档续页一致性

- 发现：同步状态文件与已发布目标硬链接时，发布失败前的写入可污染旧目标；远端映射后的 SQLite 游标误用本地路径命名空间，会使 200+ 候选后的归档续页重复或遗漏。
- 交付：所有变更文件及同步状态均在写入前解除硬链接；SQLite 续页游标保留原始 `rollout_path` 命名空间，实际读取仍使用已校验的本地映射路径。
- 验证：`npm run verify` 通过：236 项 Node、12 项 Chromium；服务健康。
- 结论：已复核的安全、资源、快照、分页、同步与无障碍主题，没有其他达到明确、可复现、完整交付门槛的问题；仅新运行证据、用户反馈或可复现缺陷重新启动。

## 2026-07-18 第十六轮：可达性与首次使用引导

- 发现：移动端在任务归档模式仍可切入被隐藏的复核台，留下无内容且键盘/读屏状态错误的面板；默认本机 Codex 为空但自动发现的 Pi Agent 有会话时，页面把用户留在“没有匹配”空态；筛选与模块按钮冒充 tab，部分真实 tab 也没有完整的 tab/panel 关联。
- 交付：提交 `d82b4bd`。任务归档提供可读的复核状态，退出归档或打开会话后恢复会话复核，并隔离迟到详情响应；默认实时、无筛选的空本机 Codex 只对自动发现的 Pi 来源执行一次同范围有界探测，确认有结果后显示显式切换入口，不自动切源或扫描历史；真实内容/复核/设置视图补齐双向 tab/panel 契约，时间分类改为 radio，模块切换改为 toolbar 按下按钮，README 与 Chromium 回归同步。
- 验证：独立执行 `npm run verify` 通过：236 项 Node、15 项 Chromium；`codex-session-renderer.service` 为 `active`，`GET http://127.0.0.1:4789/api/health` 返回 `ok:true`。
- 下一轮建议：当前已复核安全、资源、快照、分页、同步、移动可达性及首次使用路径，未见另一个达到明确、可复现、可完整交付门槛的改进包。后续仅在新增真实用户反馈、远端快照联调证据（成功、认证失败、超时）或新的可复现缺陷出现时重新启动。

## 2026-07-18 第十七轮：交付后收敛复审

- 审视：独立产品评审复查第十六轮提交、运行服务、真实本机空 Codex/Pi 有会话场景、桌面与移动路径、键盘/读屏语义、远端历史、任务归档、有界诊断及安全响应头。
- 结论：没有发现另一个同时具备明确复现条件、实际用户影响和完整验收路径的改进包；不将无真实远端 peer 的环境假设或纯视觉偏好记为缺陷。
- 验证：独立确认服务 `active` 且健康接口 `ok:true`；`npm test` 236 项和隔离 Chromium 15 项均通过，测试后工作区干净。
- 后续触发：仅在获得真实远端快照成功、认证失败或超时链路证据，真实移动/读屏用户可复现阻塞，或带请求条件、预期与实际结果的新缺陷时，再启动下一轮演进。

## 2026-07-18 第十八轮：大 Pi 会话任务归档前缀提取

- 发现：Pi 会话 `019f7428-ed4e-7fbd-b385-2ee046fc106b` 约 4.8 MiB，首个用户任务在第 4 个事件；旧归档因总文件超过 2 MiB 直接标记 `too_large`，使按项目回看、搜索和辨识首任务失效，尽管有界 JSONL 前缀已经足以安全读取该任务。
- 交付：提交 `fd270c5`。归档统一在既有 2 MiB、20,000 事件和 4 路并发预算内读取前缀，找到首个有效任务即停止；只有前缀内未找到时才返回 `file_too_large` 或 `too_many_events`。保留签名校验、取消、共享读取与缓存边界；大文件成功条目不使用可能来自前缀外索引的用户派生标题。README、Node、HTTP 与 Chromium 回归同步。
- 验证：独立执行 `npm run verify` 通过：242 项 Node、16 项 Chromium。运行中 Pi 归档接口将该会话返回为 `found`，首任务在事件 4，正文 2,926 字符、预览 280 字符，标题为“未命名会话”；服务为 `active` 且健康接口 `ok:true`。
- 下一轮建议：继续以真实运行证据审视当前闭环；优先等待受控远端快照的成功、认证失败和超时链路，或新增用户可复现缺陷。前缀后才出现的首任务仍受既有预算限制，不返回部分正文。

## 2026-07-18 第十九轮：Pi Goal 控制消息安全投影

- 发现：Pi Goal-mode 将启动、更新、恢复和自动续跑控制包持久化为 `role:user` 事件。完整包含 `goal_id`、规则和 continuation marker；旧实现会把它作为默认阅读、审计、搜索、归档和 Markdown 的用户任务，第十八轮的大 Pi 会话也会受影响。
- 交付：提交 `39307ed`。新增严格的 Pi session v3 / `pi-goal@0.15.1` 结构投影：完整启动或更新包仅投影真实 `<goal_objective>`，恢复和自动续跑不产生用户消息；字段不完整、版本未知、关系错误或相似真实用户输入一律原样保留。Turn、审计、摘要/搜索、归档、标题和 Markdown 共用分类，移除旧前缀误删；原始事件诊断保留原始 payload。README 与事件规范同步。
- 验证：独立执行 `npm run verify` 通过：244 项 Node、17 项 Chromium。运行实例的 turns、compact、audit、Markdown 和归档搜索均不含控制包；原始事件第 4 条仍可按需读取原始控制记录。服务为 `active`，健康接口 `ok:true`。
- 下一轮建议：当前 Pi 首任务、默认投影和原始诊断边界已闭环。仅在获得其他 Pi Goal 协议版本的真实样本、远端快照端到端证据或新增可复现用户缺陷时继续演进；未知协议版本保留原文，避免误删。

## 2026-07-18 第二十轮：交付后收敛复审

- 审视：独立评审完整复查第 1 至 19 轮边界、最新投影实现、前后端交互、资源与快照处理、测试夹具及运行实例；覆盖本机 Codex、Pi-only、大会话归档、移动/键盘/读屏和远端操作者工作流。
- 结论：没有找到同时具备真实复现证据、明确用户伤害、完整交付边界和可验收测试的另一项改进包。未把无远端 peer 的现场假设、纯视觉偏好或原始事件诊断保留语义登记为缺陷。
- 验证：运行实例为当前工作区，健康接口 `ok:true`，本机 Codex 与 Pi 来源可用；真实 Pi 全量归档扫描 189 会话约 0.282 秒，14.37 MiB 会话受限详情保持有界诊断而归档仍从事件 4 提取首任务；默认搜索不命中 Goal 规则或续跑标记，安全响应头齐全。
- 后续触发：仅在取得真实远端快照成功、认证失败或超时链路，出现其他 Pi Goal 协议样本，或有带复现条件的新移动、键盘、读屏、归档或诊断阻塞时，重新开启演进。

## 2026-07-18 第二十一轮：Codex Goal 控制包安全投影

- 发现：真实 Codex 会话 `019f6171-1263-7e32-a1a1-61edd4bdd777` 的 `response_item` 事件 9 是完整 `source="goal"` 自动续跑包。旧实现将其作为首个用户任务、默认阅读/审计/导出/搜索内容，并使用 SQLite 中同一控制文本作为列表标题，影响扫描与读屏。
- 交付：提交 `c6ff289`。以已实证的 `codex-thread-goal@0.144.1` 模板、session 双 ID、active 状态、UUID turn 关联与完整单段 input 文本严格识别；首次可信 objective 仅投影一次，后续同目标续跑包抑制，任何未知或结构不完整文本保留原文。默认派生面共用投影；轻量列表仅对疑似控制标题做 24 条/96 KiB 前缀探测与签名校验，原始事件保留。
- 验证：独立执行 `npm run verify` 通过：247 项 Node、18 项 Chromium。运行实例的 turns、audit、Markdown 和归档搜索不含控制包；解析后的原始事件 9 仍含完整控制记录。服务为 `active`，健康接口 `ok:true`。
- 下一轮建议：继续等待可复现的协议新变体、受控远端联调或真实用户阻塞；未知控制协议默认保留，防止误删真实输入。

## 2026-07-18 第二十二轮：历史会话稳定标题

- 发现：同一本机 Codex 会话在 `all` 显示 SQLite 标题、在 `history` 却显示 rollout 文件名；真实会话 `019f4efa-2334-78c1-824b-585122747778` 如此，172 个交集 ID 标题不同。历史行的可见文本、搜索词和读屏名称均受影响，且详情标题与左侧不一致。
- 交付：提交 `eb4d58b`。对已通过路径和 ID 校验且受当前列表上限约束的文件回退候选，批量补齐 SQLite 标题，不读取更多 JSONL 或详情；recent/all/history、详情、query、Markdown 和归档使用稳定标题路径，修复跨 scope 缓存顺序依赖。Pi 标题、严格 Goal 投影、远端仅索引与路径失败关闭不变；README、事件规范和移动/ARIA 回归同步。
- 验证：独立执行 `npm run verify` 通过：248 项 Node、19 项 Chromium。运行实例中上述会话的 all、history、detail、query 均为“你能使用终端工具吗？”，工作区干净，服务为 `active` 且健康接口 `ok:true`。
- 下一轮建议：当前标题模型、控制包投影和历史读取边界已闭环；仅在真实远端链路、协议新样本或用户可复现的浏览/无障碍阻塞出现时继续演进。

## 2026-07-18 第二十三轮：会话列表长标题边界

- 发现：历史列表实际返回 186 条约 1.14 MiB，75 条标题超过 1,000 字符；会话 `019f3fd4-0613-7d31-adce-800a582747f0` 标题为 60,065 字符，直接进入桌面/移动可见行和 `aria-label`，使焦点、读屏、渲染和浏览器搜索不可用。
- 交付：提交 `83e1e09`。将规范标题与列表展示标题分离，所有来源列表只返回最多 160 个 Unicode 字符的 `displayTitle` 及截断状态；DOM、ARIA 和选择反馈只使用有界值，完整规范标题仅在详情、导出、归档、Raw 和外部 query 按需保留。服务端仍用完整标题检索，Pi/Goal、历史预算和远端仅索引/路径边界不变；README、事件规范及桌面/390px 回归同步。
- 验证：独立执行 `npm run verify` 通过：252 项 Node、20 项 Chromium。运行中历史列表不含完整 `title`，该会话 `displayTitle` 为 160 字符且标记截断，详情仍为 60,065 字符，完整后段关键词可命中；服务为 `active` 且健康接口 `ok:true`。
- 下一轮建议：当前列表的信息密度、标题一致性、无障碍名称与完整详情边界已闭环；后续仅接受真实远端链路、协议新样本或带复现条件的用户阻塞。

## 2026-07-18 第二十四轮：长标题类型筛选完整性

- 发现：第 23 轮后列表只含截断标题，但“错误/工具”类型仍在浏览器匹配 `displayTitle`。历史 `error` 规范标题命中 43 条而 39 条被前端遗漏，`tool` 13 条全部遗漏；真实会话 `019f5fcc-c7c9-7820-a197-4b4ec551add8` 的关键词在标题第 527 个字符后。
- 交付：提交 `3d3f7c7`。本机、远端 SQLite 与文件回退在完整规范标题、cwd、relativePath 上服务端执行 `type=all|error|tool`，截断后才返回；远端分页快照绑定类型，前端切换类型重新请求且不再二次过滤。列表、DOM、ARIA 和状态公告不回传完整标题/匹配词，Goal、Pi、预算、仅索引与路径边界不变；README 和回归同步。
- 验证：独立执行 `npm run verify` 通过：254 项 Node、21 项 Chromium。运行中上述会话可由 `error` 和 `tool` 类型命中，返回仅含有界 displayTitle 且不泄露完整标题或匹配词；服务为 `active`，健康接口 `ok:true`。
- 下一轮建议：当前长标题的展示、检索和类型筛选链路闭环；只在真实远端联调、协议新样本或用户复现阻塞出现时继续。

## 2026-07-18 第二十五轮：交付后收敛复审

- 审视：独立复查第 1 至 24 轮边界、最新类型筛选实现、列表/远端索引/分页契约、Goal 投影、资源与快照安全、前端移动和读屏路径及运行实例。
- 结论：没有新的同时具备运行复现、明确用户伤害、完整交付边界和可验收测试的改进包；未将纯视觉偏好、原始诊断保留语义或无真实远端 peer 的假设登记为问题。
- 验证：运行进程指向当前仓库且服务 `active`，首页与健康接口为 `200`、安全响应头齐全；真实本机 `all/error/tool` 分别返回 186/67/34 条，所有列表项不含规范 `title` 且 `displayTitle` 不超过 160 字符。最近全量门禁为 254 项 Node、21 项 Chromium 通过。
- 后续触发：仅在取得真实远端快照成功、认证失败或超时证据，出现新的 Pi Goal 协议样本，或收到具备复现步骤、预期和实际结果的移动、键盘、读屏、归档或诊断阻塞时重启演进。

## 2026-07-18 第二十六轮：来源异步隔离收尾与断点闭环

- 发现：来源响应是否还能写回当前导航，与该请求是否仍需结束自身 loading 被混为一个条件。同一来源远端刷新后切换阅读、复核或面板时，旧结果虽被正确拒绝，但刷新控件仍禁用；移动端归档在切换复核面板后也会保留“正在整理任务归档”和 `aria-busy=true`。此外浏览器此前容忍成功响应缺失来源标识，和服务端 source-scoped API 契约不一致；`820px` 的 JS/CSS 边界在小数宽度存在空档。
- 交付：将“请求仍拥有状态”和“请求仍可写回导航”拆分。刷新或归档的同源旧响应不再更新来源、数据、成功 toast 或成功状态，但拥有请求的收尾会清理 loading、恢复控件和 ARIA，并显示“已取消，可再次拉取/重新整理”。sessions、远端 index、归档和刷新成功响应要求顶层 `source.id` 精确匹配；集合条目若带 `sourceId` 必须一致；详情要求 `session.sourceId` 精确匹配，失败进入原有可重试终态。桌面与移动统一以 `820px` 为唯一 CSS/JS 边界。
- 验证：新增 Chromium 覆盖同来源刷新中的阅读/复核导航、移动归档转复核、缺失/顶层冲突/条目冲突/详情冲突来源响应，以及 820px、820.5px 最近可用视口、821px 的布局和 inert/ARIA 语义。`npm run lint`、`npm test`（254 项）、`npm run test:e2e`（31 项）和 `npm run verify` 全部通过；bulk suppression 的每条计数均未增加。

## 2026-07-19 第二十八轮：列表、归档、历史与移动导航状态机收口

- 发现：同一来源的新列表或成功远端快照不会使已完成的任务归档批次失效；已有详情时筛选会自动打开结果首项并在移动端跳离会话面板；本机“更早”失败文案承诺刷新可重试但顶部实际只读 recent；390px 仍暴露只改变桌面 `aria-expanded`、却不驱动移动面板的左右开关。
- 交付：按来源与时间范围失效归档缓存，清空旧批、取消旧请求并在归档打开时重新整理；保留已有 `selectedSessionKey`，筛选/历史加载只改左侧列表，初次无选择才自动选首项；顶部刷新在本机更早分类真实请求 `history`，失败空态提供可聚焦的“重试更早会话”；移动断点隐藏桌面左右开关并移出焦点序列，桌面恢复后继续使用原有真实展开语义。
- 验证：新增 Chromium 回归覆盖归档旧/新批、390px 筛选后保留 333 详情且显式打开 888、history 首次失败后的真实重试及不误开 recent、桌面到移动断点的开关隐藏与状态恢复；`npm run verify` 通过（254 项 Node、37 项 Chromium），`git diff --check` 通过。

## 2026-07-19 第三十轮：远端刷新取消回执与实际副作用一致性

- 发现：远端快照刷新在同来源阅读、视图、复核、范围或移动面板导航后，会在迟到响应到达时显示“已取消”，但浏览器 POST 未绑定取消信号；服务端因此收不到断连，下载、解包甚至原子发布仍可能继续，造成用户回执与实际副作用不一致。
- 交付：刷新请求持有独立 `AbortController`，并在来源、范围、会话、侧栏、主视图、复核页和移动面板等既有导航上下文真正变化后中止自己的 POST。服务端既有断连→共享订阅→下载/解包/staging 取消链保持不变：同源其他订阅者仍可完成，最后一个订阅者离开才停止工作；原子提交点之后不回滚已完整发布的快照。
- 验证：新增 Node 真 HTTP 回归，验证客户端中止 POST 后服务端刷新状态收尾、staging 清理且没有 `current` 发布；既有资源回归继续验证最后一个共享订阅取消时远端 transport 收到 abort。Chromium 使用真实慢速远端验证来源切换和同源主视图导航均产生 `net::ERR_ABORTED`，同时恢复刷新入口、`aria-busy` 与取消状态。README 同步取消语义与提交边界。独立复跑 `npm run verify`：255 项 Node、42 项 Chromium 全过；`git diff --check` 及运行实例健康检查通过。
- 提交与后续：`cb63878 修复远端快照刷新取消传递`，实例已重启为 `active`。下轮优先在真实远端 peer 可用时观察成功、超时及主动取消；其余只凭新复现证据启动，避免把纯偏好登记为缺陷。

## 2026-07-19 第二十九轮：范围请求隔离、索引刷新与断点焦点闭环

- 发现：本机 recent 列表在切换到更早历史后仍可写回并提前解除刷新忙碌状态；列表与归档仅校验来源而未严格校验请求范围；复核台调整条跨 1280px/移动断点会把焦点留在隐藏元素；远端历史刷新误读本机 sessions 快照，导致已显示的 101 条索引被清空。
- 交付：列表请求绑定数据源、范围导航版本和取消控制器，过期响应及 finally 均不可改写当前范围的列表、自动选择、忙碌状态或刷新入口，首个自动选择仅取 `filteredSessions`；recent、history 与归档响应严格校验 `scope` 并失败关闭。远端非实时刷新改为从 index 首页重读并保留可用索引/分页，不请求 sessions。调整条和桌面开关跨断点会把焦点迁移到可见的复核开关或当前移动面板 tab。README、检查脚本与 Chromium 回归同步。
- 验证：新增 5 项 Chromium 覆盖桌面及 390px 的 recent→history 迟到响应、三类 scope 错配、1281→1280→820 焦点迁移和 office 101 条索引刷新；`npm run verify` 通过（254 项 Node、42 项 Chromium），`git diff --check` 通过。

## 2026-07-19 第三十一轮：远端快照成功后的历史索引恢复

- 发现：远端来源停留在“近一天”或“更早”历史索引页时，“拉取远端快照”成功回调无条件执行 `loadSessions()`。该调用会清空 `remoteIndexSessions` 和分页缓存、请求实时快照会话，再间接重读历史索引；用户短暂看到空列表，已在第 2 页时还会丢失分页语境，违背历史仅索引、不取正文的边界。
- 交付：快照成功后按开始操作时的来源、导航、范围和索引筛选围栏恢复。历史索引上下文只从当前筛选条件的无令牌首页重建 `index`，保留旧页直至新首页确认，绝不请求 `sessions`；新首页的 opaque `snapshot` 才可继续翻页。快照拉取失败保留当前索引页、页码和令牌，不额外读取 `sessions` 或 `index`。派生索引请求绑定刷新取消信号，导航或筛选变化会中止它且不写回成功状态；请求自身仍会清理 loading，保持既有取消、来源/范围隔离和索引失败保留策略。
- 验证：新增 Chromium 覆盖远端“近一天”和“更早”索引第 2 页：成功拉取后重建第 1 页、不请求 `sessions`、后续页只携带新快照令牌；失败拉取后保留原第 2 页和分页且不产生额外 `sessions`/`index` 请求。新增 Node 静态契约保护刷新分支、取消信号和文档承诺。README 同步刷新语义和回归范围；`npm run verify` 通过（256 项 Node、45 项 Chromium），`git diff --check` 通过。

## 2026-07-19 第三十二轮：交付后收敛审查

- 审视：独立只读复查第三十、三十一轮的远端“近一天/更早”第 2 页刷新成功、刷新失败、筛选变化、来源切换、同源导航和 390px 面板切换，并追溯浏览器取消、服务端订阅、共享下载、staging 清理、索引 snapshot 分页及 ARIA 回执。
- 结论：没有新的、同时具备直接实现/测试证据、可复现用户伤害和完整验收边界的改进主题。成功恢复已仅重读无令牌索引首页，旧页在到达前保留，后续页使用新 snapshot；失败不额外读 `sessions`/`index`；取消会中止 POST 且最后订阅取消才停止传输。
- 验证：独立 `npm run verify` 通过（256 项 Node、45 项 Chromium），`git diff --check` 通过，`codex-session-renderer.service` 为 `active`，`/api/health` 返回 `ok:true`。
- 后续触发：仅在真实远端 peer 的成功、认证失败、超时或主动取消回执不一致，真实高延迟下发生索引页/令牌混合，或真实 390px 键盘/读屏用户报告焦点、公告、控件可达性阻塞时重开演进。
