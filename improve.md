# 项目质量改进记录

## 2026-07-17 第一轮：局域网访问边界

### 背景与发现

- 本轮开始时仓库没有既有 `improve.md`，以当前代码、README、近期提交和全量质量门禁为基线进行侦察。
- 最高优先级问题是：systemd 示例曾将主工作台监听到 `0.0.0.0`，而主服务没有访问控制；同网段访问者可读取会话正文、原始事件、Markdown、项目路径，并能管理或触发远端数据源操作。
- 其他已识别但未在本轮处理的方向：任务归档首次加载的大规模 JSONL 并发解析峰值；响应式布局、焦点管理和键盘操作缺少真实浏览器回归测试。

### 已交付

- 提交 `6be4d5f 加固局域网工作台访问控制`。
- 新增统一访问控制：loopback 监听保持零配置；非 loopback 监听必须提供 `CODEX_SESSION_RENDERER_TOKEN`，缺失时拒绝启动。静态资源与全部 API（包括远端配置、连接测试和刷新）统一认证。
- 支持浏览器 HTTP Basic（用户名 `codex`）和脚本 Bearer 认证；使用常量时间秘密比较，且不会在响应、错误或公开配置中回显 token。
- 远端数据源配置目录/文件收紧为当前用户权限，写入原子化并串行化；新增 CSP、防嗅探、防嵌入、无缓存等响应头。
- README 与 systemd 示例同步为默认 loopback，并补充经 token 保护的局域网部署方式。

### 验证证据

- `npm run lint`：通过。
- `npm test`：通过，187 项测试全部成功；覆盖非 loopback 启动拒绝、静态/读取/写入路由认证、错误凭据、token 脱敏、配置权限与安全响应头。
- 运行中实例：`codex-session-renderer` 为 active，仅监听 `127.0.0.1:4789`；`GET /api/health` 返回 200 并确认 `cache-control: no-store`、CSP、`nosniff` 等响应头。

### 下一轮建议

1. 优先评估“任务归档首次加载的资源上限”：聚焦大规模/超长 JSONL 会话首次归档时的并发、内存和取消语义，保持现有增量缓存和时间分类体验。
2. 随后评估浏览器级回归：针对 README 已承诺的桌面/窄屏布局、焦点恢复、键盘导航和面板尺寸调整建立高价值的真实交互验证。

## 2026-07-17 第二轮：任务归档资源边界

### 背景与发现

- 原任务归档在首次访问时对至多 800 个会话执行无界 `Promise.all`，每个会话都完整读取 JSONL；前端固定请求 800 条并一次性渲染完整正文。
- 负载复现表明，120 个首提示词各 512 KiB 的会话已产生约 60 MiB 响应和约 570 MiB RSS；客户端取消不会停止服务端读取。
- 原 in-flight 去重未以文件版本为键，读取中写入会话文件时可能将旧结果写入新缓存范围。

### 已交付

- 提交 `10655f5 限制任务归档首次加载并支持取消`。
- 新增归档协调器：每次最多发现/返回 200 个会话，服务实例最多 4 路 JSONL 读取并发，缓存最多 400 个当前文件版本；单文件限制为 2 MiB、20,000 条非空记录和 12,000 个提示词字符。
- 超大文件、超长提示词、过多记录和读取中变化的会话会返回明确状态且不带正文；读取前后签名不一致时不缓存旧数据。
- 前端以 `AbortController` 取消过期归档请求；服务端把 HTTP 断连转为订阅取消。相同文件签名共享读取，只有最后一个订阅者取消才中止底层流。
- README 与 JSONL 规范文档记录了资源上限、缓存版本键、状态含义和取消行为。

### 验证证据

- `npm run lint`：通过。
- `npm test`：通过，195 项测试全部成功；新增覆盖慢读取并发上限、跨请求配额、单/多订阅取消、读取中修改、超限正文隐藏和缓存命中。
- 实际运行实例：`codex-session-renderer` active，主进程 `3959692`，监听 `127.0.0.1:4789`；健康接口正常。

### 下一轮建议

1. 优先审视并建立最小真实浏览器回归：页面已承诺的响应式三栏/移动面板、键盘 tablist、弹窗焦点恢复、复核台尺寸调整，以及归档取消后的加载状态，都尚未经过真实 DOM 与浏览器布局验证。
2. 同时重新扫描当前基线，确认浏览器级验证仍是最高价值主题；若发现更高优先级的功能、安全或数据正确性问题，应以可复现证据调整方向。

## 2026-07-17 第三轮：Chromium 工作台契约

### 背景与发现

- README 已承诺三栏/窄屏/移动面板切换、键盘 tablist、隐藏面板焦点迁移、设置弹窗焦点恢复、复核台宽度调整和归档取消行为，但此前只有 Node 纯函数测试与源码文本断言。
- 侦察在 Chromium 中确认这些行为当前可用，同时确认仓库没有项目私有的浏览器依赖、`test:e2e` 脚本或可在 CI 复现的隔离页面测试环境。

### 已交付

- 提交 `4928193 新增 Chromium 工作台契约回归测试`。
- 新增项目私有 Playwright Chromium 配置、浏览器安装脚本、临时服务和固定 JSONL 样本；测试不读取真实 `~/.codex`、浏览器配置或用户会话。
- 四个端到端场景覆盖桌面三栏与复核台键盘调宽、窄屏流式复核与移动三面板、四类 tablist 的键盘/ARIA 同步及焦点恢复、归档请求取消与过期响应隔离。
- 新增 `install:e2e-browser`、`test:e2e` 和 `verify` 脚本；测试失败保留 trace、截图和视频诊断产物。README 已同步安装、运行和覆盖范围说明。

### 验证证据

- `npm run lint`：通过。
- `npm test`：通过，194 项断言测试成功。此前的 195 项包含一个没有测试用例的辅助 fixture 文件；限定为 `test/*.test.mjs` 后不再将其计作测试文件，实际测试覆盖未减少。
- `npm run test:e2e`：通过，4/4 Chromium 场景成功。
- 实际运行实例：`codex-session-renderer` active，主进程 `4025277`，监听 `127.0.0.1:4789`，健康接口正常。

### 下一轮建议

1. 重新进行全维度侦察，而非预设后续主题。当前安全边界、归档资源边界和关键浏览器交互均已有专门回归保护；优先寻找仍有明确用户影响、可复现证据且适合一个完整交付的缺口。
2. 若没有达到该门槛的改进点，应记录已检查维度与残余风险，压缩 `improve.md`（如接近 300 行）并结束本次无人值守迭代。

## 2026-07-17 第四轮：会话详情资源边界

### 背景与发现

- 详情、外部 view 和 Markdown 导出此前会无上限读取完整 JSONL、构建事件/Turn/Trace/Audit，并把详情对象无限期保存在每个数据源的缓存中。
- 隔离复现中，一条包含 3 MiB 用户消息的会话返回约 6.3 MiB 详情响应；连续打开不同大文件会同时推高服务内存、网络传输、浏览器渲染和缓存占用。

### 已交付

- 提交 `9078f46 限制会话详情读取并完善取消缓存边界`。
- 新增统一详情协调器，约束详情、compact 子会话、Markdown 和原始事件读取：默认单文件 8 MiB、10,000 事件、服务级 4 路并发，以及 24 条/48 MiB 的版本 LRU 缓存；配置项已文档化。
- 读取前后校验文件签名。超限、文件变化、失败与中止不会返回局部详情或污染缓存；含子会话的父详情不缓存，避免子会话变化导致 compact 过期。
- HTTP 断连及前端切换会话/数据源会取消详情、view、Markdown 和原始事件读取。超限详情会返回 `complete: false` 与 `readState`，前端明确提示受限、禁用 Markdown，并引导使用原始事件分页诊断。

### 验证证据

- `npm run lint`：通过。
- `npm test`：通过，199 项测试成功；新增覆盖服务级并发、最后订阅者取消、文件读取中变化、限额/LRU、递归子会话闸门及相关 HTTP 路由。
- `npm run test:e2e`：通过，4/4 Chromium 场景成功。
- 实际运行实例：`codex-session-renderer` active，主进程 `4089805`，监听 `127.0.0.1:4789`，健康接口正常。

### 下一轮建议

1. 进行最终全维度侦察，确认本轮安全、归档、详情、浏览器交互和数据源可靠性基线后，是否还存在具备明确用户影响和可复现证据的完整主题。
2. 已知残余风险包括远端快照下载尚未具备明确 deadline/下载体积预算、共享端快照生成尚未全局门控，以及大型前端文件的历史复杂度债务；若不能证明其当前用户影响或形成完整低风险交付，本轮不应为重构而重构。

## 2026-07-17 第五轮：远端快照传输与共享建包

### 背景与发现

- 两个查看端同时请求共享快照时会同时复制、打包同一会话树；远端快照下载、历史索引和健康检查此前没有 deadline、响应体积预算或端到端取消。
- 快照发布曾通过两次目录重命名切换 `current`，读取者可能短暂观察到目录不存在，README 的“原子发布”保证强于实际实现。

### 已交付

- 提交并推送 `5616d23 加固远端快照传输与共享建包资源边界`；本地 `main` 与 `origin/main` 已对齐。
- 远端快照、索引和健康请求新增 deadline、声明长度预检、流式实际字节限制与取消传递；同源请求共享，跨源由进程级闸门限制并发。
- 共享端等价建包请求合并并使用受限队列；客户端断连可中止复制/打包并清理临时目录。
- 快照发布改为不可变版本目录与原子 `current` 指针切换，读取者只会得到完整旧版本或完整新版本；失败不会覆盖旧快照，旧版本保留数量受限。
- README 已同步远端资源环境变量、边界和发布语义。

### 验证证据

- `npm run lint`：通过。
- `npm test`：通过，206 项测试成功；新增覆盖声明/chunked 超量响应、超时、取消、跨源并发、共享端请求合并/队列/取消与原子指针读取。
- `npm run test:e2e`：通过，4/4 Chromium 场景成功。
- 运行与发布：`main...origin/main` 无差异；`codex-session-renderer` active，主进程 `4148243`，监听 `127.0.0.1:4789`；健康和数据源接口返回正常。

### 本次迭代结论

- 已连续完成主服务访问边界、归档加载、浏览器交互回归、详情加载和远端快照传输五个具备复现证据的完整主题。
- 当前不再主动扩展到缺少用户影响证据的前端大文件重构或其他泛化优化；后续应从真实运行监控、用户反馈或新的可复现缺陷重新启动质量迭代。

## 2026-07-17 第六轮：快照内容展开预算

### 背景与交付

- 收口审查发现快照下载虽限制压缩包，但共享端建包与查看端解包缺少展开后字节、文件数和 deadline 预算，异常高压缩比归档或 `scope=all` 可耗尽临时磁盘。
- 提交并推送 `1622fd8 完善快照内容展开预算`：共享端与查看端均增加总字节、文件数、产物和时间预算；流式解包拒绝压缩炸弹、链接、路径逃逸、重复及非白名单条目；超限、取消或 deadline 会清理临时目录并保留旧快照。
- README 已同步限制与语义。`npm run lint`、`npm test`（212 项）、`npm run test:e2e`（4 项）和 `npm run verify` 均通过；实例已重启，服务 active 且健康接口正常。

### 迭代结论

- 本次已完成六个由独立侦察发现并验证的完整质量主题。后续只应在新的真实运行证据、用户反馈或可复现缺陷出现时继续迭代，避免无依据的泛化重构。

## 2026-07-17 第七轮：远端来源版本隔离

### 背景与发现

- 同一 `sourceId` 的远端地址或快照路径变更后，旧实现会继续复用固定的 `current` 快照目录；刷新合并键也只使用快照根目录。
- 因此旧来源的快照可能被新来源显示，且旧 in-flight 刷新有机会与新配置共用任务或向同一 `current` 发布。名称修改和 token 轮换与实际来源变更没有被区分。

### 已交付

- 为远端数据源新增不含 token 的来源版本：由规范化快照地址或快照路径、远端 Codex Home 和本机快照根共同计算；名称和 token 变更不会改变版本。
- 状态文件持久化来源版本。只有 `current` 与当前来源版本匹配时才视为可用；不匹配的旧快照会进入“需要拉取新快照”状态，列表、详情、归档、复核、查询和 Markdown 导出统一返回受控 `409`，不会读取旧内容。
- 刷新订阅、staging 目录和发布版本目录均带来源版本；服务进程维护活动版本围栏。页面配置地址变更或删除会先让旧围栏失效，旧刷新不能复用、发布或覆盖新来源，且旧任务返回可识别的 `source_configuration_changed` 结果。
- 配置保存/删除接口返回 `configuration.result`（`source_changed`、`source_unchanged`、`source_removed`）和是否需要刷新。页面在来源变更后清空当前会话、禁用旧导出并明确提供“拉取远端快照”入口；同源改名或 token 轮换保留现有快照。

## 2026-07-18 第八轮：远端历史索引分页闭环

### 背景与交付

- 远端共享端和主服务已经提供索引 `page.total/cursor/nextCursor`，但工作台此前只渲染首批 `sessions`，远程审阅者无法判断结果是否完整，也无法继续定位更早记录。
- 前端现在按服务端 cursor 请求每个有界页，显示当前范围、总数和页号；前进/返回替换当前页，末页明确显示无更多结果。最近 6 个已访问页使用有限 LRU 缓存，返回这些页不重新请求。
- 来源、时间分类、搜索词和会话类型变更会中止旧 `fetch`、清空页状态并用查询键隔离过期响应。远端历史模式只渲染当前索引页，不与快照列表混合；索引行保持不可打开、不可导出和“未同步正文”提示。
- 新增 Node 覆盖共享端分页与主服务代理字段透传；Chromium 夹具启动真实 token 保护的远端索引服务，覆盖 101 条索引的翻页、末页、缓存回退、取消和过期响应隔离。

### 验证证据

- `npm run lint`：通过。
- 定向 Node 回归：16 项通过。
- `npm run test:e2e`：8/8 Chromium 场景通过。

### 保留边界

- 远端历史索引仍只提供元数据；不会扩大实时快照窗口、同步正文或改变来源版本隔离和远端资源预算。
- README 已记录版本判定字段、`409` 状态、页面操作提示及同源失败仍可保留旧快照的边界。

### 验证证据

- `npm run verify`：通过。`npm run lint`、Node 语法检查、214 项 Node 测试和 4/4 Chromium 契约测试全部成功。
- 新增高价值回归覆盖：来源版本不包含 token 且对地址/快照路径/远端 Home/快照根敏感；跨 registry 改来源时旧 in-flight 刷新返回 `409`、新刷新独立发布；HTTP 配置保存覆盖同源配置保留、变地址后列表与 Markdown 阻断及脱敏配置结果。

### 下一轮建议

1. 优先评估详情受限会话的工作台内分页诊断：现有服务端已支持受限的原始事件分页，但页面当前只能跳转裸 JSON 接口。应在不放宽读取预算的前提下，让用户在工作台内有界翻页、按需查看单条来源，并覆盖取消和过期响应隔离。
2. 远端历史索引超过首批上限时，评估是否补足“已加载数量、继续加载、结束态”闭环；保持索引只含元数据，不把它扩展为历史正文同步协议。
3. 除上述已具备复现证据的方向外，安全、资源边界、快照一致性和关键浏览器交互均已有专项保护。没有新的真实运行证据、用户反馈或稳定复现时，不进行泛化重构。

## 2026-07-17 第八轮：远端快照落盘安全与来源状态事务

### 背景与发现

- 第七轮的来源围栏会在配置文件原子替换之前失效；一次配置写入失败会提前隔离内存中的旧来源任务和已验证快照。
- 快照状态文件直接覆盖写入时，崩溃或磁盘异常可能留下截断 JSON；仅凭损坏状态文件无法安全判断固定 `current` 是否属于当前来源。
- 快照版本目录、解包/复制内容和 SQLite 元数据没有统一 Unix 权限收紧，默认 `umask` 不足以保证会话正文、工具输出和路径私有。

### 已交付

- 配置 store 改为在配置文件原子提交成功后才通知来源围栏；提交失败不会修改 registry，旧同源快照仍可读、旧刷新仍按原语义运行。
- 每个不可变快照版本写入仅含来源版本的内部证明；状态文件以私有临时文件加原子替换写入。状态文件缺失、异常或截断时，只在 `current` 的证明匹配当前来源时恢复可读，否则维持隔离。
- Unix 上对默认/自定义快照根、staging、versions、已发布内容、SQLite、索引、状态和证明收紧到目录 `0700`、常规文件 `0600`；已有快照树会在加载时修正。
- 新增分层回归：真实 mode 断言覆盖默认和自定义根，损坏状态的同源恢复/错源拒绝，配置替换失败不会触发来源围栏。

### 下一轮建议

1. 优先评估受限会话的工作台内原始事件分页诊断，复用现有服务端读取与取消预算，不扩大正文传输范围。
2. 继续以真实运行证据为入口；当前远端快照的来源隔离、落盘权限、传输/展开预算和原子发布均已有专项测试，不做无依据重构。

## 2026-07-17 第九轮：远端最终发布 TOCTOU 收口

### 背景与发现

- 第七轮的最终 `assertSourceIdentityCurrent` 与异步 `rename(nextLink, current)` 之间仍可让出事件循环。旧刷新若在此窗口停住，改址后的新刷新可以先成功发布，旧刷新恢复后仍会覆盖固定的 `current`，且两个调用都会报告成功。

### 已交付

- 新增进程内、按规范化快照根划分的最终提交槽。下载、解包、复制和校验保持并行；最终来源判定与 `current` 指针替换在不让出事件循环的同步提交中完成，配置提交后的来源围栏更新进入同一提交模型。
- 配置原子替换后立即更新围栏，临时文件清理不再位于两者之间；替换失败仍不会改变旧围栏。
- 旧来源在最终边界被改址或删除时返回 `409/source_configuration_changed`，不再触发 `current` 替换；同根新版本可独立准备并发布，不同根仍可同时到达最终发布边界。
- README 同步了线性化边界、同根协调与不同根并行语义，且不会暴露 token。

### 验证证据

- 新增确定性回归：旧任务停在最终发布前，改址并完成新刷新后释放旧任务，旧任务返回 `409` 且 `current` 仍为新来源；删除围栏的同窗口场景保持旧 `current`；两个不同快照根可并发到达最终发布边界。
- `npm run lint`、`npm run check` 已通过；完整 `npm run verify` 作为本轮最终门禁执行。

### 下一轮建议

1. 优先评估受限会话的工作台内原始事件分页诊断，复用既有读取与取消预算，不扩大正文传输范围。
2. 若出现跨进程同时操作同一快照根的部署方式，再评估引入文件锁；本轮协调器的保证范围是单个 Node 服务进程。

## 2026-07-18 第十轮：受限会话的工作台内有界诊断

### 背景与发现

- 详情读取已正确在 8 MiB/10,000 事件边界返回 `complete:false`，服务端也已有 source-scoped 事件分页；但页面会把四个主视图一起替换成占位，并把诊断留为裸 JSON 链接，用户离开原会话、数据源和复核语境后才能继续取证。
- 原分页在文件超过读取字节边界时可能继续报告 `hasMore:true`，下一次扫描会得到不推进的空页；连续分页也没有令牌阻止远端新快照或本地文件新版本和旧页混合。
- 交付后的第十轮复查发现：分页诊断可越过 `CODEX_SESSION_DETAIL_MAX_EVENTS` 展示摘要，但单条来源读取复用了完整详情的同一事件上限，导致已展示的后续事件不能回读完整来源。

### 已交付

- 原始事件主视图在详情受限时改为“有界原始事件诊断”：保留会话标题、当前数据源和受限标记，仅加载事件摘要；前进使用服务端游标，返回保留最多 6 页本地页历史，不会预取或累计完整 JSONL。
- 分页响应新增不泄露路径/正文的 `snapshot` 令牌；续页不匹配返回 `409/session_snapshot_changed`。文件变化、快照变化、网络失败或重试都会清空旧页，避免混合内容。字节边界现返回 `hasMore:false`、`truncated:true` 和明确停止原因。
- 选中摘要后才可在复核台“来源”页调用既有单条 source-scoped 原文接口；诊断、单条原文在切换视图、会话、数据源、刷新、详情重试时均可取消，陈旧响应不会写回。
- 新增独立的 `CODEX_SESSION_DIAGNOSTIC_MAX_EVENT_SCAN`（默认 100,000 条逻辑事件）作为分页与单条来源共同的随机访问边界；它不改变完整详情的 `CODEX_SESSION_DETAIL_MAX_EVENTS`。分页抵达边界会停止并标记 `raw_event_scan_limit`，直接越界读取受控返回 `413/session_event_scan_limited`，因此页面实际展示的摘要均可携带原快照令牌回读。
- README 同步了界面、API、令牌、字节边界与 Chromium 覆盖说明；`improve.md` 保持低于 300 行。

### 验证证据

- Node 回归覆盖 opaque snapshot 解析、连续游标、摘要默认不含 payload/raw、旧 snapshot `409`、字节边界停止，以及详情事件上限之后的摘要可回读和独立诊断扫描上限拒绝。
- Chromium 覆盖受限详情内嵌诊断、下一页/本地返回、详情上限之后的摘要按需读取完整事件，以及离开诊断后的取消与陈旧响应隔离。

### 下一轮建议

1. 仅在真实运行证据显示需要时，评估远端历史索引的“继续加载/已结束”闭环；保持索引不传正文。
2. 结合生产会话分布观察默认 100,000 条诊断索引边界是否需要调整；任何调整仍应保持单文件字节、并发、快照令牌、取消和数据源隔离约束。