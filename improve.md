# 项目质量演进记录

## 2026-07-17 第一轮：局域网访问边界

- 发现：主工作台曾可监听 `0.0.0.0`，但没有访问控制；同网段访问者可能读取会话内容、路径和远端数据源信息，或触发管理操作。
- 交付：提交 `6be4d5f`。非 loopback 监听要求 `CODEX_SESSION_RENDERER_TOKEN`；静态资源和全部 API 统一认证，支持 Basic/Bearer，采用常量时间比较。配置文件权限、原子写入、安全响应头及 README/systemd 示例同步收紧。
- 验证：`npm run lint`、`npm test`（187 项）通过，覆盖启动拒绝、认证、脱敏、配置权限和安全响应头。

## 2026-07-17 第二轮：任务归档资源边界

- 发现：首次归档对最多 800 个 JSONL 使用无界 `Promise.all` 并完整读取；120 个 512 KiB 首提示会产生约 60 MiB 响应和约 570 MiB RSS，客户端取消不会停止服务端读取。
- 交付：提交 `10655f5`。归档发现/返回上限 200、服务读取并发上限 4、版本缓存上限 400；单文件、记录数和提示词长度均受限。引入文件版本键、读取前后签名校验、订阅级取消和最后订阅者中止底层流。
- 验证：`npm run lint`、`npm test`（195 项）通过，覆盖并发、取消、文件变化、超限和缓存命中。

## 2026-07-17 第三轮：Chromium 工作台契约

- 发现：README 已承诺响应式面板、键盘 tablist、焦点恢复、复核台调宽和归档取消，但此前没有真实浏览器回归。
- 交付：提交 `4928193`。新增隔离的 Playwright Chromium 配置、固定 JSONL 夹具、临时服务和 `install:e2e-browser`、`test:e2e`、`verify` 脚本；不读取真实用户数据。覆盖桌面/窄屏/移动布局、键盘与 ARIA、焦点恢复、归档取消和过期响应隔离。
- 验证：`npm run lint`、`npm test`（194 项）及 `npm run test:e2e`（4 项）通过。

## 2026-07-17 第四轮：会话详情资源边界

- 发现：详情、外部 view、Markdown 和原始事件会无界读取完整 JSONL，并将详情无限期缓存；一条 3 MiB 用户消息可返回约 6.3 MiB 响应。
- 交付：提交 `9078f46`。统一详情协调器限制单文件 8 MiB、10,000 事件、服务并发 4、LRU 24 条/48 MiB；校验文件版本，超限/变化/失败/中止不返回局部详情或污染缓存。前后端传播取消，受限详情给出 `complete:false` 与 `readState`。
- 验证：`npm run lint`、`npm test`（199 项）和 `npm run test:e2e`（4 项）通过。

## 2026-07-17 第五轮：远端快照传输与共享建包

- 发现：共享端的等价快照请求会重复复制和打包；远端快照下载、历史索引、健康检查缺少 deadline、字节预算和端到端取消；旧 `current` 发布切换存在短暂不可见窗口。
- 交付：提交 `5616d23`。远端请求支持 deadline、声明长度预检、流式字节限制和取消；同源请求共享、跨源受进程级闸门控制。共享端合并等价建包并限制队列，发布改为不可变版本目录与原子 `current` 指针切换，保留数量受限。
- 验证：`npm run lint`、`npm test`（206 项）和 `npm run test:e2e`（4 项）通过。

## 2026-07-17 第六轮：快照内容展开预算

- 发现：压缩包下载受限后，建包和解包仍缺少展开字节、文件数和 deadline 预算，异常压缩比归档或 `scope=all` 可耗尽临时磁盘。
- 交付：提交 `1622fd8`。共享端和查看端均限制总字节、文件数、产物和时间；流式解包拒绝压缩炸弹、链接、路径逃逸、重复和非白名单条目，超限、取消或 deadline 均清理临时目录并保留旧快照。
- 验证：`npm run lint`、`npm test`（212 项）、`npm run test:e2e`（4 项）和 `npm run verify` 通过。

## 2026-07-17 第七轮：远端来源版本隔离

- 发现：同一 `sourceId` 改变远端地址或快照路径后，固定 `current` 可能继续展示旧来源；刷新合并键只按快照根计算，旧 in-flight 刷新可能影响新配置。
- 交付：为远端来源计算不含 token 的版本标识，持久化至状态文件。仅当前来源版本匹配时读取快照；不匹配时列表、详情、归档、复核、查询和 Markdown 统一返回受控 `409`。刷新、staging、发布目录和活动围栏均绑定来源版本，前端来源变化后清空旧会话并阻断旧导出。
- 验证：回归覆盖来源版本敏感字段、改址后的旧刷新 `409`、新刷新独立发布，以及同源改名/token 轮换保留快照。

## 2026-07-17 第八轮：远端快照落盘安全与来源状态事务

- 发现：来源围栏曾在配置原子替换前失效，配置写失败也会隔离旧快照；状态文件直接覆盖可能截断，且快照树和 SQLite 元数据的权限未统一收紧。
- 交付：仅在配置原子提交成功后更新来源围栏；状态文件以私有临时文件原子替换。状态损坏时仅在 `current` 内部证明匹配当前来源时恢复读取。Unix 下收紧快照根、staging、版本内容、SQLite、索引、状态和证明的权限。
- 验证：覆盖配置替换失败不改变围栏、损坏状态的同源恢复/错源拒绝及默认、自定义快照根的真实 mode。
- 同期索引闭环：远端历史模式按服务端 cursor 加载有界元数据页，显示范围、总数、页号和结束态，最近 6 页使用 LRU。筛选变更会取消旧请求并隔离过期响应；索引行保持不可打开、不可导出，不扩大为历史正文同步。

## 2026-07-17 第九轮：远端最终发布 TOCTOU 收口

- 发现：最终来源校验与异步替换 `current` 之间会让出事件循环；旧刷新可能在改址后覆盖新来源已发布的 `current`。
- 交付：增加按规范化快照根划分的进程内最终提交槽。最终来源判定与 `current` 指针替换在线性化同步提交中完成；配置成功后立即更新围栏。旧来源在最终边界改址或删除时返回 `409/source_configuration_changed`，不同根仍可并行准备。
- 验证：确定性覆盖旧任务在最终发布前暂停、改址后新发布成功、旧任务恢复返回 `409` 且不覆盖新 `current`，以及删除和不同根并行情形。

## 2026-07-18 第十轮：受限会话的工作台内有界诊断

- 发现：详情受 8 MiB/10,000 事件限制时，页面会用占位替换主视图并跳转裸 JSON；分页可能在读取字节边界后不推进，且缺少令牌阻止快照/文件变化时混合旧页。单条来源读取复用详情事件上限，已展示的后续摘要无法回读原文。
- 交付：受限详情改为工作台内有界原始事件诊断，按服务端游标分页、最多保留 6 页本地历史，按需读取单条来源。分页采用 opaque `snapshot`，不匹配返回 `409/session_snapshot_changed`；字节边界停止并声明原因。新增独立的 `CODEX_SESSION_DIAGNOSTIC_MAX_EVENT_SCAN`（默认 100,000），保障已展示摘要均可回读；切换页面、会话、数据源、刷新和重试均取消旧请求并隔离陈旧响应。
- 验证：Node 覆盖 snapshot、游标、摘要脱敏、`409`、字节边界和诊断扫描上限；Chromium 覆盖内嵌诊断、翻页/本地返回、按需读取、取消和陈旧响应隔离。

## 2026-07-18 第十一轮：远端历史索引完整性

### 发现

- SQLite 查询存在 1000 条静默截断，远端历史索引无法如实表达完整匹配范围。
- 数值 cursor 在索引数据变动后会跨变动混合页面，用户可能把不同时间点的结果当作同一列表。
- 索引查询与传输缺少完整的有界和取消约束，存在无界执行、无取消传播的资源风险。

### 交付

- 将 count、筛选和稳定分页下推至 DB 侧，使总数与每页范围基于同一筛选条件计算。
- 以 opaque snapshot 绑定续页；快照不匹配时返回 `409`，由调用方恢复至新的稳定查询，而不混合旧页。
- 为查询和传输补齐 deadline、取消传播与回退预算，确保异常、断连和受限路径均在有界资源内结束。

### 验证

- `npm run verify` 通过：230 项 Node 测试、9 项 Chromium 测试通过。

### 下一轮建议

1. 优先让任务归档的 200 条范围对用户透明，并继续定位仍会影响归档完整性的缺口。
2. 使用可复现的 Linux 测试判断 71 同步 staging 是否存在硬链接污染。
3. 补强无障碍异步状态与对比度。